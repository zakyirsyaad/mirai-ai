Use the Mirai MCP tools to show current campaign status.

Call `mirai_get_campaign`, then summarize:
- campaign status and mode
- connected X handle
- expiry
- planned, posted, skipped, and failed counts

If the tool returns an error, explain the actionable next step.
