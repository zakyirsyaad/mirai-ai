Use the Mirai MCP tools to activate a license.

If the user included a license key in the message, call `mirai_activate_license` with it. If not, ask for the Mirai license key. After activation, call `mirai_healthcheck` and summarize service, expiry, and campaign availability.
