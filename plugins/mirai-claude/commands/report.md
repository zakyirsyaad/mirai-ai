---
description: Generate a Mirai campaign proof-of-work report.
---

Use the Mirai MCP tools to generate the campaign report.

Call `mirai_get_report`, then summarize campaign window, connected X handle,
planned/posted/skipped/failed counts, tweet URLs, and metrics when available.
Do not expose secrets or raw tokens.
